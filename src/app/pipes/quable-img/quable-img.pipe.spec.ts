import { QuableImgPipe } from './quable-img.pipe';

describe('QuableImgPipe', () => {
  it('create an instance', () => {
    const pipe = new QuableImgPipe();
    expect(pipe).toBeTruthy();
  });
});
